# Congestion Control Identifier

## Running the identifier
Install project dependencies and create a virtual env with Poetry:
`poetry install`

Run scripts with Poetry:
`poetry run python example.py`
